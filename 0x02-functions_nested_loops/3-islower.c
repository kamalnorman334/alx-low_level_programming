#include "main.h"

/**
 * _islower - checks for lower case characters
 * @c: entry
 *
 * Description: prints lowercase letters
 * Return: Always(0).
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
