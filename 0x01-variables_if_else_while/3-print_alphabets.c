#include <stdio.h>
/**
 * main - print alphabet in lower and uppercase
 *
 *
 * Return: (0) Success
 */
int main(void)
{
char chr;
for (chr = 'a' ; chr <= 'z' ; chr++)
{
char(chr);
	}
	for (chr = 'A' ; chr <= 'Z' ; chr++)
	{
		putchar(chr);
	}
		putchar('\n');
	return (0);
}
