#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/* 
 * main - Point entry
 * Description - assign a random number to the variable
 *
 * Return: 0
 *
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if int(n > 5)


	return (0);
}
